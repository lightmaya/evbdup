 <%= render :partial => '/shared/ztree/ztree_show',:locals => { :act => "index", :hide_tree => (current_user.department.is_childless? || current_user.department.descendants.where.not(status: 404).blank?), :get_path => ztree_kobe_departments_path, :post_path => move_kobe_departments_path, :current_node_id => @dep.id, :ajax_show_url => (edit_kobe_user_path(params[:u_id]) if params[:u_id].present?), :ztree_opt => ["增加","修改","删除","冻结","恢复"] } %>