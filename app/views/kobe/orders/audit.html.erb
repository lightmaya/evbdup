<%= show_tabs_with_name("#{@order.name} #{@order.status_badge}", @arr, 'order_tab') %>
<%= render partial: '/kobe/shared/audit', locals: { action: update_audit_kobe_order_path(@order), obj: @order, title: "审核: #{@order.name}" } %>
