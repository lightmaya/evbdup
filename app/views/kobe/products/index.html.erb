<div class="panel panel-grey margin-bottom-40">
    <div class="panel-heading">
        <h3 class="panel-title"><i class="fa fa-tasks"></i> 产品管理</h3>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-8 pull-left padding-top-5">
                <%= link_to '<i class="fa fa-plus"></i> 新增</a>'.html_safe, new_kobe_product_path %>
            </div>
            <div class="col-md-4 pull-right">
            <%= search_form_for @q, url: kobe_products_path, html: { method: :get } do |f| %>
                <div class="margin-bottom-10">
                    <div class="input-group">
                        <%= f.search_field :brand_or_model_or_version_cont, class: "form-control input-sm", placeholder: "请输入项目名称..." %>
                        <span class="input-group-btn">
                          <button class="btn btn-success btn-sm" type="submit">搜索</button>
                        </span>
                    </div>
                </div>
            <% end %>
            </div>
        </div>
        <%= modal_dialog("opt_dialog") %>
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th class="index">序号</th>
                    <th class="hidden-sm">品牌</th>
                    <th class="hidden-sm">型号</th>
                    <th class="status_bar"><%= status_filter(Product) %></th>
                    <th class="date"><%= date_filter %></th>
                    <th class="cando">操作</th>
                </tr>
            </thead>
            <tbody>
                <% @products.each_with_index do |product,index| %>
                <tr>
                    <td><%= show_index(index) %></td>
                    <td><%= product.brand %></td>
                    <td><%= product.model %></td>
                    <td><%= product.status_bar %></td>
                    <td><%= show_date(product.created_at) %></td>
                    <td><%= btn_group(products_btn(product)) %></td>
                </tr>
                <% end %>
            </tbody>
        </table>
        <%= paginate @products %> 
    </div>                     
</div>