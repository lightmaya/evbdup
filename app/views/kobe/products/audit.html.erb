<%= show_tabs_with_name("#{@product.item.name} [#{@product.department.real_dep.name}]", @arr, 'product_tab') %>
<%= render partial: '/kobe/shared/audit', locals: { action: update_audit_kobe_product_path(@product), obj: @product, title: "审核产品: #{@product.name}" } %>