	<%= render :partial=> 'rs_table' ,locals: { title: "品目统计", name: "品目", result: @categories, obj: @ca, total: @category_total } %>
	<%= render :partial=> 'rs_table' ,locals: { title: "单位明细统计", name: "单位名称", result: @departments, obj: @dep, total: @department_total } %>
	<%= render :partial=> 'rs_table' ,locals: { title: "采购方式统计", name: "方式", result: @yw_types, obj: @dep, total: @yw_total } %>

