<%= draw_myform(@myform) %>

<%= render partial: '/kobe/shared/budget_money', locals: { obj_id: @bargain.try(:budget_id), input_id: "bargains_total", budget_id: "bargains_budget_id" } if @bargain.try(:department_id) == current_user.department.id %>

<script type="text/javascript">
  $(document).ready(function(){

    // 选择预算审批单 给budget_id和budget_money赋值
    $("body").on("click", ".dialog_box input[name='bargains_total'][type='radio']", function(){
      var budget_money = $(this).nextAll('.red').text();
      $("#new_bargain #bargains_total").val(budget_money);
    });

    // 产品的关键参数至少选择一个
    // $(".details_part .input_part:visible").each(function() {
    //   // body...
    // });

});
</script>
