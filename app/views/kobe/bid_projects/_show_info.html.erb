<%= show_tabs_with_name("#{@bid_project.name} #{@bid_project.status_badge}", @arr, 'bid_project_tab') %>

<%= render partial: 'bid_list', locals: { act: 'show' } if !@bid_project.can_bid? && @bpbs.present? %>
